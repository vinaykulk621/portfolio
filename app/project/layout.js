export const metadata = {
  title: 'Projects',
  description: 'Portofolio website',
}

export default function home({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
