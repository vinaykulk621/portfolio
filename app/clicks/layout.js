export const metadata = {
  title: "Clicks",
  description: "Portofolio website",
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <head />
      <body>{children}</body>
    </html>
  )
}
